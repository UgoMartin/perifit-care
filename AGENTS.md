# Repository instructions

Avant toute intégration UI ou Figma dans `App/`, lire et appliquer `rules/README.md` et `rules/perifit-ds.mapping.json`, puis exécuter `node rules/validate-perifit-ds-mapping.mjs`. Ne pas consulter ni utiliser `old/`.
